package com.care.wei;

import java.util.Map;

public class Test  {/*
	// ①通过匿名内部类覆盖ThreadLocal的initialValue()方法，指定初始值
	private static ThreadLocal<Map> seqNum = new ThreadLocal<Map>() {
		public Map initialValue() {
			return Map.Entry<key, value>;
		}
	};

	// ②获取下一个序列值
	public int getNextNum() {
		seqNum.set(seqNum.get() + 1);
		return seqNum.get();
	}

	public static void main(String[] args) {
		Test sn = new Test();  
		TestClient sn1 = new TestClient(sn);
		TestClient sn2 = new TestClient(sn);
		TestClient sn3 = new TestClient(sn);
		sn1.start();
		sn2.start();
		sn3.start();
	}


*/}
